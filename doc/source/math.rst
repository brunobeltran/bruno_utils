.. _math:

Math utilities
==============

Currently, the only useful function here is a "locally-linear" fit routine that
works as a "rolling-window" style filter. Can accomplish similar tasks to
fitting a Gaussian process for when there's simply way too much data for that to
be viable.

.. automodule:: bruno_util.math
    :noindex:

